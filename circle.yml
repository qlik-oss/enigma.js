machine:
  node:
    version: 6.9.5

general:
  artifacts:
    - "coverage"
    - "dist"
    - "jsdoc"

dependencies:
  override:
    - yarn
    - npm run build

test:
  override:
    - npm run test
    - npm run lint
    - npm run jsdoc
